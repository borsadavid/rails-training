<h1>Edit User</h1>

<%= form_with model: @user, url: update_user_path(@user), method: :patch do |f| %>
  <div>
    Name: <%= f.text_field :name %>
  </div>

  <div>
    Email: <%= f.text_field :email %>
  </div>

  <%= f.submit "Update" %>
<% end %>

<%= link_to "Back", users_path %>